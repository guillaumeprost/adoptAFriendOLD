{% extends 'base.html.twig' %}


{% block body %}
    <div class="raw">
        <div class="col-md-12">
            <h1>Créer une nouvelle annonce</h1>

            <div class="raw">
                <div class="col-md-12">
                    {{ render_esi(path('offer_render_form', {'type': 'dog'}), options = []) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}